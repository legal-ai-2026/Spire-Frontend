when:
  branch: main
  event: push

steps:
  - name: test-backend
    image: python:3.11-slim
    commands:
      - pip install --no-cache-dir -r backend/requirements.txt aiohttp
      - python -m compileall backend server.py
      - python -c "from backend.app.main import create_app; app = create_app(); print(app.title)"

  - name: test-frontend
    image: node:20-slim
    commands:
      - cd frontend
      - npm ci
      - npm run build

  - name: build-and-push
    image: gcr.io/kaniko-project/executor:debug
    depends_on:
      - test-backend
      - test-frontend
    commands:
      - /kaniko/executor --dockerfile=Dockerfile --context=. --destination=ci-cd-zot.ci-cd.svc.cluster.local:5000/c2d2-fe:latest --destination=ci-cd-zot.ci-cd.svc.cluster.local:5000/c2d2-fe:$(echo $CI_COMMIT_SHA | cut -c1-8) --insecure --skip-tls-verify

  - name: deploy-staging
    image: bitnami/kubectl:latest
    depends_on:
      - build-and-push
    commands:
      - export SHORT_SHA=$(echo $CI_COMMIT_SHA | cut -c1-8)
      - kubectl config set-cluster homelab --server=$K8S_SERVER --insecure-skip-tls-verify=true
      - kubectl config set-credentials deployer --token=$K8S_TOKEN
      - kubectl config set-context homelab --cluster=homelab --user=deployer --namespace=staging
      - kubectl config use-context homelab
      - sed -i "s|IMAGE_TAG|$SHORT_SHA|g" k8s/staging/deployment.yaml
      - kubectl apply -f k8s/staging/deployment.yaml
      - |
        i=0
        while [ "$i" -lt 36 ]; do
          DESIRED=$(kubectl get deployment/c2d2-fe --namespace=staging -o jsonpath='{.spec.replicas}')
          UPDATED=$(kubectl get deployment/c2d2-fe --namespace=staging -o jsonpath='{.status.updatedReplicas}')
          AVAILABLE=$(kubectl get deployment/c2d2-fe --namespace=staging -o jsonpath='{.status.availableReplicas}')
          if [ -z "$DESIRED" ]; then DESIRED=1; fi
          if [ -z "$UPDATED" ]; then UPDATED=0; fi
          if [ -z "$AVAILABLE" ]; then AVAILABLE=0; fi
          echo "deployment status desired=$DESIRED updated=$UPDATED available=$AVAILABLE"
          if [ "$UPDATED" = "$DESIRED" ] && [ "$AVAILABLE" = "$DESIRED" ]; then
            kubectl get deployment/c2d2-fe --namespace=staging
            exit 0
          fi
          i=$((i + 1))
          sleep 5
        done
        kubectl get deployment/c2d2-fe --namespace=staging
        exit 1
    environment:
      K8S_TOKEN:
        from_secret: K8S_TOKEN_STAGING
      K8S_SERVER:
        from_secret: K8S_SERVER
